import React from 'react';

const Navigation = () => (
  <nav className='navigation__wrapper'>
    <a href="#">Home</a>
    <a href="#">About</a>
    <a href="#">Resume</a>
    <a href="#">Skills</a>
    <a href="#">Projects</a>
  </nav>
);

export default Navigation;
