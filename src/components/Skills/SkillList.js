import React, { Fragment } from 'react';

// Components
import SkillIcon from './SkillIcon.js';

const SkillList = () => (
  <Fragment>
    <SkillIcon
      type={"ruby"}
      level={5}
    />
  </Fragment>
);

export default SkillList;
